import React from 'react';
import '../styles/Settings.css';

const Settings: React.FC = () => {
  return (
    <div className="settings">
      <h2>Settings</h2>
      {/* Settings for sound, animations, etc. will go here */}
    </div>
  );
};

export default Settings;
